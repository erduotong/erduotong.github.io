---
import BaseHead from '@/components/common/BaseHead.astro'
import '../styles/global.css'
import DarkModeAdapter from '@/components/common/DarkModeAdapter.astro'
import SkipToMainContent from '@/components/common/SkipToMainContent.astro'
import Footer from '@/components/footer/Footer.astro'
import Header from '@/components/header/Header.astro'
import { SITE_TITLE } from '@/const.ts'

interface Props {
	// 有title就拼接为 title | SITE_TITLE
	// 没有则就 SITE_TITLE
	title?: string
	description: string
}
const title = Astro.props.title
	? `${Astro.props.title} | ${SITE_TITLE} `
	: SITE_TITLE
---
<!doctype html>
<html lang="zh">
    <!--suppress HtmlRequiredTitleElement -->
    <head>
        <BaseHead title={title} description={Astro.props.description}></BaseHead>
        <DarkModeAdapter></DarkModeAdapter>
    </head>
    <body>
        <SkipToMainContent href="#main-content"></SkipToMainContent>
        <Header></Header>
        <div id="mobile-navigation-container"/>
        <slot />
        <Footer></Footer>
    </body>
</html>

<style>
    html,
    body {
        margin: 0;
        width: 100%;
        min-height: 100%;
    }
</style>
