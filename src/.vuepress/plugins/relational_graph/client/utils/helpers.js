// 工具函数
export function isPathMatch(routePath, nodePath) {
  // 路径匹配逻辑...
}

export function getTransformedMousePosition(event, canvas, transform) {
  // 获取转换后的鼠标位置...
}

export function findClickedNode(x, y, nodes) {
  // 查找被点击的节点...
}

// 其他辅助函数... 